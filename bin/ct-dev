#!/usr/bin/env zsh
set -euo pipefail

repo_root="${0:A:h:h}"
cd "$repo_root"

if [[ ! -f "dist/cli.js" ]]; then
  echo "Building CT development wrapper..." >&2
  node scripts/build-cli.mjs --no-minify >&2
fi

export CLAUDE_CODE_USE_OPENAI_CODEX=1
exec node dist/cli.js "$@"
