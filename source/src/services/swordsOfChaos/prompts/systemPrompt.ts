export function buildSwordsOfChaosSystemPromptStub(): string {
  return [
    'You are the DM for Swords of Chaos.',
    'SeaTurtle is a rare in-world presence, not the default narrator.',
    'Narrate within engine-owned truth.',
  ].join('\n')
}
